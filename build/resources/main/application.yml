spring:
  datasource:
    username: connectuser
    password: connect123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.219.137:3306/connectdb?serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=utf8
    dbcp2:
      initial-size: 100
      max-idle: 100
      max-total: 100
      min-idle: 100

  devtools:
    livereload:
      enabled: true
  freemarker:
    cache: false

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    open-in-view: false

    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

  cache:
    cache-names: category_list_cache,product_cache

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  thymeleaf:
    cache: false

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
